name: VDS Command Center — Pages

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'command-center/**'
      - 'api/v1/**'
      - 'scripts/prepare_command_center_assets.py'
      - '.github/workflows/vds-command-center-pages.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: vds-command-center-pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Stage static VDS app
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .pages
          mkdir -p .pages/api
          cp -R command-center/. .pages/
          if [ -d api/v1 ]; then
            mkdir -p .pages/api/v1
            cp -R api/v1/. .pages/api/v1/
          fi
          python3 scripts/prepare_command_center_assets.py .pages

      - name: Static smoke test
        shell: bash
        run: |
          set -euo pipefail
          test -s .pages/index.html
          test -s .pages/assets/app.css
          test -s .pages/assets/app.js
          test -s .pages/assets/fonts/dm-sans.woff2
          test -s .pages/assets/fonts/material-symbols-rounded.woff2
          test -s .pages/assets/vendor/chart.umd.min.js
          grep -q "production core" .pages/index.html
          grep -q "companies.json" .pages/assets/app.js

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload VDS Command Center
        uses: actions/upload-pages-artifact@v3
        with:
          path: .pages

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
